# NEXORY Backend - Solo datos mock (sin base de datos)
# Opcional: crear .env solo si quieres cambiar estos valores.

PORT=3000
NODE_ENV=development
JWT_SECRET=change_this_secret_key
JWT_EXPIRES_IN=24h
FRONTEND_URL=http://localhost:5001
